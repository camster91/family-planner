# Production environment template. Configure real values in the hosting
# provider; never commit production secrets or a live .env.production file.
NEXT_PUBLIC_APP_URL=https://family.ashbi.ca
NODE_ENV=production
