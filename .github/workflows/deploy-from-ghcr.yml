name: Deprecated - Deploy Image to Coolify

# Superseded by deploy-coolify.yml.
#
# Retained as historical workflow context because older run records reference this
# filename. It cannot deploy. Use the protected manual workflow in
# deploy-coolify.yml after recording approval and the exact candidate identity.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deployment path retired
    runs-on: ubuntu-latest
    steps:
      - name: Stop with migration guidance
        run: |
          echo "::error::This deployment path is retired. Use 'Build, Test & Deploy to Coolify' with the protected production environment."
          exit 1
